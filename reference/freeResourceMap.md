# Free memory used by a ResouceMap

The resources allocated by the redland RDF package are freed. The
ResourceMap object should be deleted immediately following this call.

## Usage

``` r
freeResourceMap(x)

# S4 method for class 'ResourceMap'
freeResourceMap(x)
```

## Arguments

- x:

  a ResourceMap

## See also

[`ResourceMap-class`](https://docs.ropensci.org/datapack/reference/ResourceMap-class.md)
